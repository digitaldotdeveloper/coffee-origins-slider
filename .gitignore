# raw Gemini renders, review screenshots and publish staging: large and local-only
gen/
shots/
publish/
# PNG/JPG masters; the site loads the WebPs tools/optimize.py builds from them into img/
art/
# the user's sample bag photos
ref/
*.bak-*
__pycache__/
